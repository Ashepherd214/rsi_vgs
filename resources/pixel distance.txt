he size for the runway canvas is determined in lib/main.dart starting around line 369.

The RunwayPainter is drawn inside a CustomPaint widget configured with size: Size.infinite, which tells it to expand to fill its parent.

Its parent is a Stack that is wrapped in a Container and an Expanded widget inside a Row:

Expanded(
  flex: 4,
  child: Container(
    height: 500, // <-- Height constraint here
    color: const Color.fromARGB(255, 1, 110, 5),
    child: Stack(
      children: [
        CustomPaint(
          size: Size.infinite, // <-- Expands to the bounds of the Container
          painter: RunwayPainter(
// ...
So the actual canvas size is constrained to:

Height: Fixed at 500 pixels by the Container.
Width: Dynamic, taking up 4 parts of the available width in the parent Row due to the Expanded(flex: 4) wrapper.
Generated by Gemini gemini-3.1-pro-preview
